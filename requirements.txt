# Full server stack — used by the Render deployment (see render.yaml).
# Vercel's serverless function uses the lighter api/requirements.txt instead.
flask>=2.0.0
flask-wtf>=1.0.0
gunicorn>=21.0.0
pandas>=1.5.0
numpy>=1.21.0,<2.3.0
pyarrow>=10.0.0
scikit-learn>=1.0.0
joblib>=1.0.0
